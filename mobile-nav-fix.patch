diff --git a/index.html b/index.html
index a59d37f..f0d26ee 100644
--- a/index.html
+++ b/index.html
@@ -1042,7 +1042,10 @@ textarea {
   .nav-toggle {
     display: inline-grid;
     place-items: center;
+    align-content: center;
     gap: 4px;
+    padding: 0;
+    cursor: pointer;
     width: 46px;
     height: 46px;
     border: 1px solid var(--line);
@@ -1078,12 +1081,23 @@ textarea {
     background: var(--white);
     border-bottom: 1px solid var(--line);
     box-shadow: var(--shadow);
+    max-height: calc(100vh - 72px);
+    max-height: calc(100dvh - 72px);
+    overflow-y: auto;
+    overscroll-behavior: contain;
+    visibility: hidden;
     transform: translateY(-130%);
-    transition: transform 220ms ease;
+    transition: transform 220ms ease, visibility 0s linear 220ms;
   }
 
   .nav-links.is-open {
+    visibility: visible;
     transform: translateY(0);
+    transition: transform 220ms ease, visibility 0s;
+  }
+
+  .nav-links a:not(.nav-cta) {
+    border-bottom: 1px solid var(--line);
   }
 
   .nav-links a {
@@ -1374,14 +1388,25 @@ textarea {
     box-shadow: 0 18px 34px rgba(37,211,102,0.35); transition: transform 180ms ease;
   }
   .floating-whatsapp:hover { transform: translateY(-3px); }
+  .back-to-top {
+    position: fixed; right: 25px; bottom: 92px; z-index: 119;
+    display: grid; place-items: center; width: 48px; height: 48px; padding: 0;
+    color: var(--white); background: var(--black); border: 2px solid var(--red); border-radius: 50%;
+    box-shadow: 0 12px 28px rgba(8,9,11,0.25); cursor: pointer;
+    opacity: 0; visibility: hidden; transform: translateY(10px);
+    transition: opacity 200ms ease, transform 200ms ease, visibility 0s linear 200ms, background 180ms ease;
+  }
+  .back-to-top.is-visible { opacity: 1; visibility: visible; transform: translateY(0); transition: opacity 200ms ease, transform 200ms ease, visibility 0s, background 180ms ease; }
+  .back-to-top:hover { background: var(--red); }
+  .back-to-top:focus-visible { outline: 3px solid var(--red); outline-offset: 3px; }
+  .back-to-top svg { width: 22px; height: 22px; }
+  body.nav-open .back-to-top { opacity: 0; visibility: hidden; }
   .floating-whatsapp svg { width: 30px; height: 30px; }
 
   @media (max-width: 860px) {
     .card-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     .strip-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     .steps { grid-template-columns: 1fr; }
-    .nav-links { display: none; }
-    .nav-toggle { display: grid; }
   }
   @media (max-width: 520px) {
     .card-grid { grid-template-columns: 1fr; }
@@ -2493,6 +2518,10 @@ textarea {
       </div>
     </footer>
 
+    <button class="back-to-top" type="button" aria-label="Back to top and menu" title="Back to top" data-back-to-top>
+      <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19V5M5 12l7-7 7 7"/></svg>
+    </button>
+
     <a class="floating-whatsapp" href="https://api.whatsapp.com/send?phone=13453274460&text=Hi%2C%20I%27d%20like%20to%20book%20a%20Quick%20Run." aria-label="Book Quick Runs Cayman via WhatsApp">
       <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M20.5 3.5A11.7 11.7 0 0 0 12.1 0 11.9 11.9 0 0 0 1.8 17.9L0 24l6.3-1.7A11.9 11.9 0 0 0 12.1 24h.1A11.9 11.9 0 0 0 20.5 3.5Zm-8.4 18.4h-.1a9.8 9.8 0 0 1-5-1.4l-.4-.2-3.7 1 1-3.6-.2-.4A9.8 9.8 0 1 1 12.1 21.9Zm5.4-7.3c-.3-.1-1.7-.8-2-.9-.3-.1-.5-.1-.7.1-.2.3-.8.9-1 1.1-.2.2-.4.2-.7.1a8 8 0 0 1-2.3-1.4 8.7 8.7 0 0 1-1.6-2c-.2-.3 0-.5.1-.7l.5-.6c.2-.2.2-.4.3-.6.1-.2 0-.4 0-.6-.1-.1-.7-1.6-1-2.2-.2-.6-.5-.5-.7-.5h-.6c-.2 0-.6.1-.9.4-.3.3-1.2 1.1-1.2 2.8 0 1.6 1.2 3.2 1.4 3.5.2.2 2.4 3.7 5.8 5.1.8.4 1.5.6 2 .7.8.3 1.6.2 2.2.1.7-.1 1.7-.7 2-1.4.2-.7.2-1.3.2-1.4-.1-.1-.3-.2-.6-.4Z"/></svg>
     </a>
@@ -2504,28 +2533,52 @@ const navLinks = document.querySelector("[data-nav-links]");
 const requestForm = document.querySelector("[data-request-form]");
 const whatsAppBase = "https://api.whatsapp.com/send";
 
+const backToTop = document.querySelector("[data-back-to-top]");
+
 function setHeaderState() {
   header.classList.toggle("is-scrolled", window.scrollY > 8);
+  backToTop.classList.toggle("is-visible", window.scrollY > 600);
 }
 
+backToTop.addEventListener("click", () => {
+  const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
+  window.scrollTo({ top: 0, behavior: reduce ? "auto" : "smooth" });
+  // Move keyboard focus to the menu (toggle on mobile, first link on desktop)
+  const target = getComputedStyle(toggle).display !== "none" ? toggle : navLinks.querySelector("a");
+  target.focus({ preventScroll: true });
+});
+
 setHeaderState();
 window.addEventListener("scroll", setHeaderState, { passive: true });
 
+function setNavOpen(open) {
+  toggle.setAttribute("aria-expanded", String(open));
+  toggle.setAttribute("aria-label", open ? "Close menu" : "Open menu");
+  navLinks.classList.toggle("is-open", open);
+  document.body.classList.toggle("nav-open", open);
+}
+
+setNavOpen(false);
+
 toggle.addEventListener("click", () => {
-  const isOpen = toggle.getAttribute("aria-expanded") === "true";
-  toggle.setAttribute("aria-expanded", String(!isOpen));
-  navLinks.classList.toggle("is-open", !isOpen);
-  document.body.classList.toggle("nav-open", !isOpen);
+  setNavOpen(toggle.getAttribute("aria-expanded") !== "true");
 });
 
 navLinks.addEventListener("click", (event) => {
-  if (event.target.closest("a")) {
-    toggle.setAttribute("aria-expanded", "false");
-    navLinks.classList.remove("is-open");
-    document.body.classList.remove("nav-open");
+  if (event.target.closest("a")) setNavOpen(false);
+});
+
+document.addEventListener("keydown", (event) => {
+  if (event.key === "Escape" && navLinks.classList.contains("is-open")) {
+    setNavOpen(false);
+    toggle.focus();
   }
 });
 
+window.matchMedia("(min-width: 881px)").addEventListener("change", (event) => {
+  if (event.matches) setNavOpen(false);
+});
+
 const reveals = document.querySelectorAll(".reveal");
 
 if ("IntersectionObserver" in window) {
